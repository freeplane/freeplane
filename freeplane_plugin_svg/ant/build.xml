<project name="freeplane_svg" default="dist" basedir="..">
	<property name="ver" value="1.0.1" />
	<property name="src" value="src" />
	<property name = "root" value = "."/>
	<property name="osgimanifest" value="${root}/META-INF/MANIFEST.MF" />	
	<property name="lib" value="${root}/lib" />
	<property name="framework.jar" value="${root}/../freeplane_framework/lib/knopflerfish/framework.jar" />
	<property name="freeplane.dist.lib" value="${root}/../freeplane/bin/dist/lib" />
	<property name="freeplane.jars" value="${freeplane.dist.lib}/freeplaneviewer.jar:${freeplane.dist.lib}/freeplaneeditor.jar:${freeplane.dist.lib}/freeplaneosgi.jar:${freeplane.dist.lib}/commons-lang-2.0.jar:${freeplane.dist.lib}/forms-1.0.5.jar:${freeplane.dist.lib}/SimplyHTML/SimplyHTML.jar" />
	<property name="src_base_for_post" value="${root}/../" />
	<property name="build" value="${root}/build" />
	<property name="dist" value="${root}/bin/dist" />
	<property name="dist.lib" value="${dist}/lib" />
	<property name="dist.osgi.dir" value="${dist}/osgi/dir" />
	<property name="dist.osgi.jar" value="${dist}/osgi/jar" />
	<property name="freeplaneplugin.jar" value="${dist.osgi.jar}/org.freeplane.plugin.svg.jar"/>
	<property name="debug" value="on" />
	<property name="java_source_version" value="1.5" />
	<property name="java_target_version" value="1.5" />
	<property name="build.compiler" value="modern" />
	<property name="classpath" value="${framework.jar}:${freeplane.jars}:${lib}/batik-awt-util.jar:${lib}/batik-bridge.jar:${lib}/batik-css.jar:${lib}/batik-dom.jar:${lib}/batik-extension.jar:${lib}/batik-ext.jar:${lib}/batik-gui-util.jar:${lib}/batik-gvt.jar:${lib}/batik-parser.jar:${lib}/batik-script.jar:${lib}/batik-svg-dom.jar:${lib}/batik-svggen.jar:${lib}/batik-swing.jar:${lib}/batik-transcoder.jar:${lib}/batik-util.jar:${lib}/batik-xml.jar:${lib}/js.jar:${lib}/pdf-transcoder.jar:${lib}/xerces_2_5_0.jar:${lib}/xml-apis.jar:${lib}/batik-squiggle.jar"/>

	<target name="build">
		<echo>Freeplane Version = ${ver}.</echo>
		<delete dir="${build}"  quiet="true"/>
		<mkdir dir="${build}" />
		<javac srcdir="${src}" destdir="${build}" classpath="${classpath}" debug="${debug}" 
			source="${java_source_version}" target="${java_target_version}">
		</javac>
	</target>


	<target name="dist" depends="build">
		<mkdir dir="${dist.osgi.dir}" />
		<copy todir="${dist.osgi.dir}">
			<fileset dir="${build}"/>
			<fileset dir="${root}">
				<include name="lib/**"/>
			</fileset>
		</copy>
		<mkdir dir="${dist.osgi.dir}/META-INF" />
		<copyfile dest="${dist.osgi.dir}/META-INF/MANIFEST.MF" src="${osgimanifest}"/>
		<delete dir="${build}"  quiet="true"/>
	</target>

	<target name="osgi_dist_as_jar" depends="dist">
		<mkdir dir="${dist.osgi.jar}" />
		<jar jarfile="${freeplaneplugin.jar}">
			<fileset dir="${dist.osgi.dir}">
				<include name="**" />
			</fileset>
		</jar>
	</target>
	
	<target name="clean">
		<delete dir="${build}"  quiet="true"/>
		<delete dir="${dist}"  quiet="true"/>
		<delete dir="${post}"  quiet="true"/>
		<delete>
			<fileset defaultexcludes="no" dir="${src}" includes="**/*~"/>
		</delete>
	</target>
	
</project>

