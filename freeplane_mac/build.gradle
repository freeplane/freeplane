repositories {
    maven {url 'http://devel.uji.es/nexus/content/repositories/releases/'}
}

dependencies {
    compile (project(':freeplane')) {
        transitive = false
    }
    
    compileOnly 'com.apple:applejavaextensions:1.5.4'
}
