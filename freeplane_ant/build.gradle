dependencies {
    compile group: 'org.apache.ant', name: 'ant', version: '1.9.3'
}

sourceSets {
    main {
        java {
            srcDir "./src"
        }
    }
}

jar {
    baseName "freeplaneant"
    includes += "./template"
    destinationDir file("./dist")
}
